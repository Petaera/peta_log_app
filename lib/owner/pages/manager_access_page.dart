import 'package:flutter/material.dart';

class ManagerAccessPage extends StatelessWidget {
  const ManagerAccessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('manager access Logs Page'),
    );
  }
}
