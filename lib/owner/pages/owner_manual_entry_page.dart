import 'package:flutter/material.dart';

class OwnerManualEntryPage extends StatelessWidget {
  const OwnerManualEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('owner manual log Page'),
    );
  }
}
