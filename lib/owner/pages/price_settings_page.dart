import 'package:flutter/material.dart';

class PriceSettingsPage extends StatelessWidget {
  const PriceSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('price Page'),
    );
  }
}
